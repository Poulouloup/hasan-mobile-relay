<?xml version="1.0" encoding="utf-8"?>
<!-- Mode Light — interface épurée pour utilisation mains libres -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/hasan_bg_primary">

    <!-- Zone principale centrée -->
    <LinearLayout
        android:id="@+id/centerZone"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/bottomZone"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Logo Hasan -->
        <ImageView
            android:id="@+id/ivLightLogo"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:src="@mipmap/ic_launcher"
            android:scaleType="centerCrop"
            android:background="@drawable/bg_avatar"
            android:layout_marginBottom="24dp"
            android:contentDescription="@string/app_name" />

        <!-- Statut principal en grand -->
        <TextView
            android:id="@+id/tvLightStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/status_wake_word"
            android:textColor="@color/hasan_text_primary"
            android:textSize="32sp"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_marginBottom="16dp" />

        <!-- Dernier message Hasan -->
        <TextView
            android:id="@+id/tvLightLastMessage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/hasan_text_secondary"
            android:textSize="20sp"
            android:gravity="center"
            android:maxLines="2"
            android:ellipsize="end" />

    </LinearLayout>

    <!-- Zone basse — bouton micro + quitter -->
    <LinearLayout
        android:id="@+id/bottomZone"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:paddingBottom="32dp"
        android:paddingTop="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Gros bouton micro rond -->
        <ImageButton
            android:id="@+id/btnLightMic"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:background="@drawable/bg_round_button"
            android:src="@drawable/ic_mic"
            android:tint="@color/white"
            android:padding="20dp"
            android:scaleType="fitCenter"
            android:layout_marginBottom="24dp"
            android:contentDescription="@string/cd_mic_button" />

        <!-- Bouton quitter le mode light -->
        <TextView
            android:id="@+id/btnExitLightMode"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/light_mode_exit"
            android:textColor="@color/hasan_text_hint"
            android:textSize="14sp"
            android:padding="8dp" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
